#!/bin/sh

# PROVIDE: open5gs_ausf
# REQUIRE: LOGIN
# KEYWORD: shutdown

#
# Add the following to /etc/rc.conf to enable:
#
# open5gs_amf_enable="YES"
#
# Optional:
# open5gs_ausf_config="%%ETCDIR%%/ausf.yaml"
# open5gs_ausf_flags=""
#

. /etc/rc.subr

name="open5gs_ausf"
rcvar="${name}_enable"

load_rc_config "${name}"

: ${open5gs_ausf_enable:="NO"}
: ${open5gs_ausf_config="%%ETCDIR%%/ausf.yaml"}
: ${open5gs_ausf_flags=""}

command="%%PREFIX%%/bin/open5gs-ausfd"
procname="${command}"
pidfile="/var/run/${name}.pid"

required_files="${open5gs_ausf_config}"

start_cmd="${name}_start"

open5gs_ausf_start()
{
	echo "Starting ${name}."
	/usr/sbin/daemon -f -p "${pidfile}" \
	    "${command}" \
	    -c "${open5gs_ausf_config}" \
	    ${open5gs_ausf_flags}
}

run_rc_command "$1"
